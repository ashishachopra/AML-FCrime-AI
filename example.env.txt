# Copy to .env. Replace every CHANGE_ME value before starting the stack.

# Secret file paths. Run `python scripts/create_local_secrets.py` to generate them.
RABBITMQ_USER=aml_app
RABBITMQ_SECRET_FILE=./secrets/rabbitmq_password
JWT_SECRET_FILE=./secrets/jwt_secret

# Gateway authentication. Keep AUTH_DISABLED=false outside isolated local testing.
AUTH_DISABLED=false
JWT_ALGORITHM=HS256
JWT_ISSUER=aml-reference
JWT_AUDIENCE=aml-api

# Optional OpenAI-assisted drafting requires BOTH explicit enablement and a key.
# Failed/uncertain calls count against the durable UTC daily attempt allowance.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini-2026-03-17
OPENAI_MAX_OUTPUT_TOKENS=700
OPENAI_TIMEOUT_SECONDS=8
SAR_GENERATION_ENABLED=false
AI_DAILY_CALL_LIMIT=100
AI_MAX_CONCURRENT=2
AI_MAX_INPUT_BYTES=8192
AI_FAILURE_THRESHOLD=3
AI_COOLDOWN_SECONDS=300
# SDK retries are always zero. AI_DAILY_CALL_LIMIT=0 prevents new paid attempts.
# For a directly run alert service, persist ALERT_DB_PATH (default data/alerts.db).
# Compose already uses its persistent /data/alerts.db volume.

# Risk policy. Calibrate and independently validate before production use.
RISK_THRESHOLD_ALERT=0.7
RISK_THRESHOLD_SAR=0.8
RISK_THRESHOLD_HIGH=0.8
RISK_THRESHOLD_CRITICAL=0.9

# Feature policy
VELOCITY_WINDOW_DAYS=30
VELOCITY_SHORT_WINDOW_DAYS=7
BASE_CURRENCY=USD
STRUCTURING_THRESHOLDS=10000
ANOMALY_MIN_HISTORY=20

# Bounded local feature processing; Compose mounts a persistent /data volume.
# When running the feature service directly, set FEATURE_DB_PATH to a writable path.
FEATURE_MAX_HISTORY_ROWS=1000
FEATURE_MAX_NETWORK_ROWS=1000
FEATURE_MAX_OUTBOX=10000
FEATURE_MAX_INFLIGHT=16
EVALUATION_TIMEOUT_SECONDS=1.0

# Versioned FATF public-statement snapshot. Refresh after each FATF plenary.
JURISDICTION_DATA_AS_OF=2026-06-19
FATF_CALL_FOR_ACTION_COUNTRIES=IR,KP,MM
FATF_INCREASED_MONITORING_COUNTRIES=AO,BA,BG,BO,CD,CI,CM,HT,IQ,KE,KW,LA,LB,MC,NP,PG,SS,SY,VE,VG,VN,YE

# Resource and processing limits
MAX_UPLOAD_FILE_BYTES=5242880
MAX_BATCH_RECORDS=10000
EVENT_PREFETCH=20
GATEWAY_MAX_INFLIGHT=16
GATEWAY_MAX_JSON_BYTES=65536
GATEWAY_MAX_BATCH_BYTES=16777216
GATEWAY_BODY_TIMEOUT_SECONDS=5
GATEWAY_REQUEST_TIMEOUT_SECONDS=35
# Per verified subject, per gateway process; use shared quotas at a distributed edge.
GATEWAY_REQUESTS_PER_MINUTE=120
GATEWAY_REQUEST_BURST=30
GATEWAY_MAX_PRINCIPALS=4096

# Graph analysis reference service
CENTRALITY_THRESHOLD=0.7
COMMUNITY_RISK_THRESHOLD=0.6
MAX_GRAPH_CONNECTIONS=1000

LOG_LEVEL=INFO
